class MapManager {
    // http request containing the map name and request options containing the format
    // has to auto-detect maps in the folder (means extensions)
    constructor(path) {
        console.log(path);
    }
}

module.exports = MapManager;